# Frontend

## Environment

Create `frontend/.env` from `frontend/.env.example` and set:

```bash
VITE_API_BASE_URL=http://localhost:8082
```

For a confirmation server, point it at the deployed backend origin instead of `localhost`.
